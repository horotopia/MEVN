# This workflow will do a clean installation of node dependencies, cache/restore them, build the source code and run tests across different versions of node
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-nodejs

name: Node.js CI

on: [push]

jobs:
  build:
    name: node_build_job
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x]
        # See supported Node.js release schedule at https://nodejs.org/en/about/releases/

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4 # Récupère le code de votre repo

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}

      - name: Install API dependencies
        run: cd api && npm ci # Installe les dépendances du projet

      - name: Lint
        run: cd api && npm run lint # Commande pour lancer votre linter, modifiez selon votre script de lint

      - name: Build
        run: cd api && npm run build --if-present

      - name: Run tests
        run: cd api && npm run test --if-present # Commande pour lancer vos tests Vitest, modifiez selon votre script de test

      - name: Install Client dependencies
        run: cd client && npm ci # Installe les dépendances du projet

      - name: Lint
        run: cd client && npm run lint # Commande pour lancer votre linter, modifiez selon votre script de lint

      - name: Build
        run: cd client && npm run build --if-present

      - name: Run tests
        run: cd client && npm run test --if-present # Commande pour lancer vos tests Vitest, modifiez selon votre script de test
